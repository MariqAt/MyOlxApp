<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ittalents.myfirstaplication">

    <application
        android:allowBackup="true"
        android:icon="@drawable/olx"
        android:label="@string/name_app"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateAccountActivity" />
        <activity android:name=".AdActivity" />
        <activity android:name=".AdminControlPanelActivity" />
        <activity android:name=".CorrespondenceActivity" />
        <activity android:name=".ActiveNoticeActivity" />
        <activity android:name=".ArchivedNoticeActivity" />
        <activity android:name=".OLXActivity" />
        <activity android:name=".SearchesActivity"></activity>
    </application>

</manifest>